const superHero: string[] = [];
const heroPower: Array<number> = [];

type Account = {
  name: string;
  isActive: boolean;
};

const allUsers: Account[] = [];

superHero.push("spiderman");
heroPower.push(8);

allUsers.push({ name: "", isActive: true });

export {};
